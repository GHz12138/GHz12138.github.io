# GHz 网站开发指南

本指南说明如何构建、测试和实时预览你的 Hextra 博客网站。

---

## 📋 环境要求

项目已包含本地 Hugo 二进制文件，但需要以下环境：

- **Go 1.20+** - Hugo 模块管理需要（[下载安装](https://golang.org/doc/install)）
- **Git** - 版本控制

验证 Go 安装：
```bash
go version
```

---

## 🚀 快速开始

### 1. 实时预览开发服务器（推荐）

这是最常用的开发模式，支持**实时热重载**：

```bash
cd /home/ghz/GHz12138.github.io
./bin/hugo server -D --disableFastRender
```

**参数说明：**
- `-D` - 显示草稿内容
- `--disableFastRender` - 完整渲染，避免预览时内容缺失
- 默认端口：`1313`

**效果：**
- ✅ 修改文件后自动刷新浏览器
- ✅ 实时看到 Markdown 和配置的更改
- ✅ 访问地址：http://localhost:1313/

### 2. 构建生产版本

生成静态网站到 `public/` 目录：

```bash
cd /home/ghz/GHz12138.github.io
./bin/hugo --minify
```

**参数说明：**
- `--minify` - 压缩 HTML/CSS/JS，减小文件体积

构建产物位于 `public/` 目录，可直接部署。

---

## 🧪 测试清单

### 本地测试步骤

1. **启动开发服务器**
   ```bash
   ./bin/hugo server -D --disableFastRender
   ```

2. **在浏览器中测试以下功能**

   | 测试项 | URL | 验证内容 |
   |--------|-----|----------|
   | 首页 | http://localhost:1313/ | 卡片布局正常、无报错 |
   | 博客列表 | http://localhost:1313/blog/ | 文章列表显示、RSS 按钮存在 |
   | 博客文章 | http://localhost:1313/blog/2025-02-13-welcome/ | 内容渲染、代码块正常 |
   | 关于页面 | http://localhost:1313/about/ | 卡片组件显示 |
   | 中文页面 | http://localhost:1313/zh-cn/ | 中文内容显示正常 |
   | 中文博客 | http://localhost:1313/zh-cn/blog/ | 中文文章列表 |

3. **功能测试**

   - [ ] **主题切换** - 右上角太阳/月亮图标切换深浅模式
   - [ ] **语言切换** - 顶部语言选择器切换中英文
   - [ ] **搜索功能** - 按 `Ctrl/Cmd + K` 或点击搜索图标
   - [ ] **代码复制** - 鼠标悬停在代码块显示复制按钮
   - [ ] **移动端** - 浏览器开发者工具切换到移动视图
   - [ ] **导航菜单** - 顶部导航各项链接正常

---

## 📝 内容编辑

### 添加新博客文章

在 `content/blog/` 目录下创建新文件，文件名格式：

```
YYYY-MM-DD-title.en.md    # 英文版本
YYYY-MM-DD-title.zh-cn.md # 中文版本
```

**示例**：
```bash
# 英文文章
nano content/blog/2025-02-14-my-post.en.md

# 中文文章
nano content/blog/2025-02-14-my-post.zh-cn.md
```

**文章 Front Matter 模板**：
```markdown
---
title: "文章标题"
date: 2025-02-14
tags:
  - 标签1
  - 标签2
authors:
  - name: GHz
    link: https://github.com/GHz12138
---

文章摘要内容...

<!--more-->

## 正文

正文内容从这里开始...
```

### 修改页面内容

| 页面 | 文件路径 |
|------|----------|
| 首页 | `content/_index.md` |
| 关于（英文） | `content/about/_index.md` |
| 关于（中文） | `content/about/_index.zh-cn.md` |
| 文档列表 | `content/docs/_index.md` |

### 自定义样式

编辑 `assets/css/custom.css` 添加你的自定义样式。

---

## 🔧 常见问题

### Q: 提示 "binary with name 'go' not found"
**A**: Hugo 需要用 Go 下载主题模块。安装 Go 后重试：
```bash
# Debian/Ubuntu
sudo apt install golang-go

# 验证
go version
```

### Q: 修改后页面没有刷新
**A**:
1. 确保使用 `./bin/hugo server -D --disableFastRender` 启动
2. 检查终端是否有错误输出
3. 强制刷新浏览器 (Ctrl+Shift+R)

### Q: 图片无法显示
**A**: 将图片放在 `static/` 目录下：
```bash
# 引用方式
![描述](/images/your-image.png)
```

### Q: 如何更新 Hextra 主题版本
**A**: 主题通过 Hugo 模块自动拉取，更新命令：
```bash
./bin/hugo mod get -u
./bin/hugo mod tidy
```

---

## 📦 部署

### GitHub Pages（自动）

本项目已配置 GitHub Actions，推送代码到 `main` 分支会自动构建并部署到：
https://ghz12138.github.io/

**手动触发**：
1. 访问 https://github.com/GHz12138/GHz12138.github.io/actions
2. 选择 "Build and Deploy" 工作流
3. 点击 "Run workflow" 按钮

### 本地构建测试

```bash
# 生成静态文件
./bin/hugo --minify

# 检查生成产物
ls -lh public/

# 本地预览构建结果（使用 Python）
cd public && python3 -m http.server 8080
```

访问 http://localhost:8080/ 预览构建后的网站。

---

## 🛠️ 开发工作流建议

```bash
# 1. 启动开发服务器
./bin/hugo server -D --disableFastRender

# 2. 在另一个终端编辑文件
nano content/blog/new-post.en.md

# 3. 保存后浏览器自动刷新（无需重启服务器）

# 4. 完成后提交
git add .
git commit -m "Add new blog post"
git push
```

---

## 📚 参考资源

- [Hextra 官方文档](https://imfing.github.io/hextra/)
- [Hugo 官方文档](https://gohugo.io/documentation/)
- [Markdown 语法指南](https://www.markdownguide.org/)
- [本站配置参考](/home/ghz/hextra) - 完整 Hextra 主题源码

---

**最后更新**: 2025-02-13
